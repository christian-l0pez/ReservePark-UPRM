package model;
    // Este enum representa los dias de la semana en los que e pueden hacer reservaciones en el sistema
public enum DiaSemana {
    LUNES,
    MARTES,
    MIERCOLES,
    JUEVES,
    VIERNES
}